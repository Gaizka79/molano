/* const firebase = require('firebase');

const fbApiKey = process.env.FB_APIKEY;
const fbAuthDomain = process.env.FB_AUTHDOMAIN;
const fbProjectId = process.env.FB_PROJECTID;
const fbStorage = process.env.FB_STORAGEBUCKET;
const fbMessage = process.env.FB_MESSAGINGSENDERID;
const fbAppId = process.env.FB_APPID;
const fbMeasurementId = process.env.FB_MEASUREMENTID;


// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: fbApiKey,
  authDomain: fbAuthDomain,
  projectId: fbProjectId,
  storageBucket: fbStorage,
  messagingSenderId: fbMessage,
  appId: fbAppId,
  measurementId: fbMeasurementId
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

module.exports = firebase; */